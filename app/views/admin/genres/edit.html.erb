<div class="container">
  <div class="row">
    <h3>ジャンル編集</h3>
  </div>
  <%= form_with model:@genre, url:admin_genre_path, method: :patch do |f| %>
    <div class="field row mt-5">
      <%= f.label:genre, "ジャンル名", class:"col-form-label" %>
      <%= f.text_field:genre, class:"form-control col-sm-3 mx-5" %>
      <%= f.submit '変更を保存', class:"btn btn-success" %>
    </div>
  <% end %>
</div>
